on_actions = { 
# ROOT is capitulated country, FROM is winner
	on_capitulation = {
		effect = {
			if = {
				limit = {
					POR = { has_country_flag = ITA_follows_through }
					original_tag = SPR
					has_war_with = POR
				}
				POR = { country_event = { id = portugalreworkmodlaresistance.34 days = 0 } }
			}
		}
	}
}