add_namespace = useful-events-quarior-create_dynamic_country
#[FROM.GetNameDefCap] demand to have a strategic area
country_event = {
	id = useful-events-quarior-create_dynamic_country.1
	title = useful-events-quarior-create_dynamic_country.1.t
	desc = useful-events-quarior-create_dynamic_country.1.d
	picture = GFX_report_event_generic_read_write
	is_triggered_only = yes
	option = {
		name = useful-events-quarior-create_dynamic_country.1.a
		trigger = {
		}
		ai_chance = {
			base = 1
		}
		create_dynamic_country = {
			original_tag = ROOT
			reserve_dynamic_country = yes
			set_temp_variable = {
				new_country = THIS
			}
		}
		random_owned_controlled_state = {
			limit = {
				is_core_of = ROOT
			}
			transfer_state_to = var:new_country
		}
	}
	option = {
		name = useful-events-quarior-create_dynamic_country.1.b
		trigger = {
		}
		ai_chance = {
			base = 1
		}
		create_dynamic_country = {
			original_tag = ROOT
			#reserve_dynamic_country = no
			set_temp_variable = {
				new_country = THIS
			}
		}
		random_owned_controlled_state = {
			limit = {
				is_core_of = ROOT
			}
			transfer_state_to = var:new_country
		}
	}
}
