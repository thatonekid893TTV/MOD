add_namespace = bavaria
country_event = {
	id = bavaria.1

	fire_only_once = yes
	
	trigger = {
		AND = {
		original_tag = GRE
		has_completed_focus = GRE_end_the_occupation
		GER = { surrender_progress > 0.75 }
		GRE = { has_war_with = GER }}
	}

	hidden = yes
	
	immediate = {
		if = { 
			limit = { 
				AND = {
		original_tag = GRE
		has_completed_focus = GRE_end_the_occupation
		GER = { surrender_progress > 0.75 }
		GRE = { has_war_with = GER }}
			}
			BAY = {
					transfer_state = 52
					transfer_state = 53
					transfer_state = 54
					add_state_core = 52
					add_state_core = 53
					add_state_core = 54
				}
				puppet = BAY
			
			
		}
		
	}
}